# 🔧 Как исправить ошибку "BOT_TOKEN не установлен"

## Проблема
Вы видите ошибку: `ValueError: BOT_TOKEN не установлен!`

Это значит, что в файле `.env` всё ещё стоят заглушки вместо реальных значений.

## Решение

### Шаг 1: Откройте файл .env

Файл `.env` находится в корне проекта (там же, где `main.py`).

**Способы открыть:**
- Двойной клик по файлу `.env` в проводнике Windows
- Откройте в любом текстовом редакторе (Блокнот, Notepad++, VS Code и т.д.)

### Шаг 2: Найдите строку с BOT_TOKEN

В файле должна быть строка:
```
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
```

### Шаг 3: Замените YOUR_BOT_TOKEN_HERE на ваш реальный токен

**Как получить токен:**
1. Откройте Telegram
2. Найдите бота [@BotFather](https://t.me/BotFather)
3. Отправьте команду `/newbot`
4. Следуйте инструкциям
5. Скопируйте токен (выглядит так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

**Замените:**
```
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
```

**На:**
```
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```
(используйте ваш реальный токен!)

### Шаг 4: Найдите строку с ADMIN_IDS

В файле должна быть строка:
```
ADMIN_IDS=YOUR_ADMIN_ID_HERE
```

### Шаг 5: Замените YOUR_ADMIN_ID_HERE на ваш ID

**Как получить ID:**
1. Откройте Telegram
2. Найдите бота [@userinfobot](https://t.me/userinfobot)
3. Отправьте команду `/start`
4. Скопируйте ваш ID (число, например: `123456789`)

**Замените:**
```
ADMIN_IDS=YOUR_ADMIN_ID_HERE
```

**На:**
```
ADMIN_IDS=123456789
```
(используйте ваш реальный ID!)

### Шаг 6: Сохраните файл

**ВАЖНО:** После изменений обязательно сохраните файл (Ctrl+S)!

### Шаг 7: Проверьте настройки

Запустите скрипт проверки:
```bash
python check_config.py
```

Если всё правильно, вы увидите:
```
✅ Основные настройки в порядке!
```

### Шаг 8: Запустите бота

```bash
python main.py
```

## Пример правильного .env файла

```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_IDS=123456789
CRYPTOBOT_TOKEN=
SUPPORT_MANAGER_ID=@VenmoSell_Manager
CHANNEL_ID=@reviews_channel
```

## Частые ошибки

❌ **Неправильно:**
```
BOT_TOKEN = 123456789:ABC...  (пробелы вокруг =)
BOT_TOKEN="123456789:ABC..."  (кавычки)
BOT_TOKEN=YOUR_BOT_TOKEN_HERE (заглушка не заменена)
```

✅ **Правильно:**
```
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

## Если всё ещё не работает

1. Убедитесь, что файл называется именно `.env` (не `.env.txt`)
2. Убедитесь, что файл находится в корне проекта (там же, где `main.py`)
3. Убедитесь, что вы сохранили файл после изменений
4. Запустите `python check_config.py` для диагностики
